import { SplideDirective } from './splide.directive';

describe('SplideDirective', () => {
  it('should create an instance', () => {
    const directive = new SplideDirective();
    expect(directive).toBeTruthy();
  });
});
